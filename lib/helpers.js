/**
 * Helper functions
 */

// Dependencies
const crypto = require('crypto')

// Defining the helpers
const helpers = {}

// Hash password
helpers.hash = fucntion (str) {
  if (typeof str === 'string' && str.length > 0) {
    const hash = crypto
  }
}

module.exports = helpers
